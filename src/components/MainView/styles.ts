import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scrollContainer: {
    flexGrow: 1,
    backgroundColor: '#fff',
    paddingBottom: 30,
  },
  centerView: {
    justifyContent: 'center',
  },
  safeAreaView: {
    alignItems: 'center',
  },
});
