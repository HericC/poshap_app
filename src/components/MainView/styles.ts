import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scrollContainer: {
    flexGrow: 1,
    backgroundColor: '#fff',
    justifyContent: 'center',
    paddingBottom: 30,
  },
  safeAreaView: {
    alignItems: 'center',
  },
});
