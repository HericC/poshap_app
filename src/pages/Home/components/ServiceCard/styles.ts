import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  container: {
    width: '90%',
    borderWidth: 1,
    borderColor: '#717F7F',
    borderRadius: 12,
    paddingBottom: 16,
    alignItems: 'center',
    backgroundColor: '#fff',
  },
});
